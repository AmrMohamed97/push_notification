 import 'package:flutter/material.dart';
 
class NextLocalNotification extends StatelessWidget {
  const NextLocalNotification({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Next Local Notification'),
      ),
    );
  }
}